/*
    Life.h
    Annabelle Huang
    CPSC 223 HW 2
    February 11th, 2023
    Purpose: declare functions 
*/ 

#include <stdbool.h>
void nextArray(int array[], int size);
bool shouldDie(int array[], size_t index, size_t size);
bool isAlive(int array[], size_t index, size_t size);
